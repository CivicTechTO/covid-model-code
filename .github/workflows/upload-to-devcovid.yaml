name: upload-to-dev
on: 
  push:
    branches:
      'main'

jobs:
  upload-to-dev:
    uses: ./.github/workflows/upload.yaml
    with:
      target: devcovid
      envKEY: ${{ secrets.PRIVATE_SSH_KEY }}
